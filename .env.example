# Solo backend/Vercel. Nunca expongas esta clave con prefijo EXPO_PUBLIC_.
FOURSQUARE_API_KEY=

# Opcional para Expo nativo o desarrollo local cuando la API vive en otra URL.
# En la web desplegada se usa /api/places/search del mismo dominio.
EXPO_PUBLIC_PLACES_API_URL=
